# awok runtime dependencies (the bb-workflow generator + web editor).
PyYAML>=6
Jinja2>=3
jsonschema>=4

# Dev/test extras (not required to run awok):
#   pip install pytest
# Front-end tests (src/scripts/tests/webedit) need `bun` + `bun install`.
