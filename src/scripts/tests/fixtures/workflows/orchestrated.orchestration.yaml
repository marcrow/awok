- ref: RECON
- for_each: recon.endpoints
  as: ep
  cap: 100
  body:
    - ref: SCAN
    - if:
        and:
          - { op: "==", left: scan.status, right: vuln }
          - { op: ">",  left: scan.risk,   right: 5 }
      then: [{ref: EXPLOIT}]
