---
name: {{ skill.name }}
description: |
{{ skill.description | indent(2, true) }}
---

# {{ skill.title or ('/' ~ skill.name ~ ' — ' ~ skill.name) }}

> ⚠️ This file is **GENERATED** from `src/workflows/{{ skill.name }}.yaml`.
> Do not edit by hand. To change it: edit the YAML then run `bb-workflow generate`.
>
> Implicit convention: each agent invocation `<name>` instructs Claude to read
> `~/.claude/agents/<name>.md` (its full instructions). No need to repeat this in
> every snippet.
{% if any_invocation_model %}
>
> **Model is not inherited.** Each invocation shows its model as `[model]` and a ⚙️
> reminder line. When you launch that agent via the `Task` tool you **must pass the
> model explicitly** (`model: <model>`) — otherwise the sub-agent silently runs on
> the session model, often a costlier one.
{% endif %}
{% if any_invocation_effort %}
>
> **Effort is set on the agent, not at launch.** An invocation that pins an `effort`
> has that effort written into the deployed agent's frontmatter
> (`~/.claude/agents/<name>.md`) by `awok deploy`, so the sub-agent applies it on its
> own. The `Task` tool has **no** `effort` argument — unlike `model`, you pass nothing
> at launch. The ⚙️ line records the effort for reference.
{% endif %}
{% if any_invocation_tools %}
>
> **Tools are set on the agent, not at launch.** An invocation that pins `tools` has
> that allow-list written into the deployed agent's frontmatter
> (`~/.claude/agents/<name>.md`) by `awok deploy`, overriding the agent's source tools
> there. The `Task` tool has **no** `tools` argument — like effort, you pass nothing at
> launch. A shared agent given conflicting pins across workflows keeps its source tools
> (a `deploy` warning flags it). The ⚙️ line records the tools for reference.
{% endif %}

Pipeline of {{ phase_count }} actions, organized into {{ group_count }} groups:
{% for gname, g in groups.items() %}`{{ gname }}` ({{ g.description }}){% if not loop.last %}, {% endif %}{% endfor %}.
{% if any_parallelism %}

---

## ⚙️ Execution protocol — order vs. parallelism

The pipeline below is a **dependency graph**, not a checklist. Two markers on each
action's header drive how you run it:

- `⇐ A, B` — this action **depends on** A and B. **Hard rule: never start it until
  every `⇐` dependency has returned** — its inputs are the files those actions wrote.
- `∥ A` — this action is **independent** of A (same stage, no edge between them).

Within that ordering, **launch independent agents together in a single message**
(one `Task` block each), never one at a time:

- actions on the **same stage** (marked `∥`), once their shared `⇐` dependency has
  returned;
- and, when one action lists several agents, all of them at once (no order between them).

Each separate message re-reads the whole accumulated context, so launching N
independent agents one-per-message multiplies cost and serializes work that could
run concurrently.
{% endif %}

{% for block in manual_blocks_at_top|default([]) %}
{{ block }}
{% endfor %}
{% if opportunistic_global and opportunistic_global.enabled %}
---

## 🧭 Opportunistic autonomy

This workflow permits **scoped improvisation**. Beyond the planned work, you (the
orchestrator) may **author and launch an ad-hoc sub-agent** whenever you spot a
signal the planned agents do not cover.

- **How**: the `Task` tool, `subagent_type: general-purpose` (or `Explore`), with a
  prompt you write yourself from context. These agents do not exist in
  `src/agents/` — you create them on the fly.
{% if opportunistic_global.when %}- **When**: {{ opportunistic_global.when|trim }}
{% endif %}- **Mode**: usually in the **background**, unless the result is needed to continue the current action.
- **Nesting limit**: a sub-agent cannot itself spawn sub-agents (max depth = 1). After reading the planned sub-agent's report, it is up to you to launch the follow-up.
- **Scope**: all actions, except those marked ⛔.
{% if opportunistic_global.examples %}
- **Examples**: {{ opportunistic_global.examples|join('; ') }}
{% endif %}
{% endif %}

---

{% if orchestration_md %}
---

{{ orchestration_md }}
{% endif %}
## Pipeline actions (DAG)

{% for phase in phases %}
### {{ phase.id }} — {{ phase.name }}
> `{{ phase.group }}` · {{ phase.type|default('agent') }}{% if phase.depends_on %} · ⇐ {{ phase.depends_on|join(', ') }}{% endif %}{% if phase.parallel_with %} · ∥ {{ phase.parallel_with|join(', ') }}{% endif %}{% if phase.triggers %} · 🔔 {{ phase.triggers|tojson }}{% endif %}

{% if phase.description %}{{ phase.description }}{% endif %}

{% if phase.signal_emissions %}
**Signals emitted by this action:**
{% for line in phase.signal_emissions %}{{ line }}
{% endfor %}

{% endif %}
{% if phase.interactive %}
> ⏸️ **Interactive checkpoint.** Present your output for this action, then **STOP and wait** for the maintainer's input/decision before continuing. Do not advance to the next action, and do not decide on their behalf.

{% endif %}
{% if phase._opp and phase._opp.note_kind == 'locked' %}
> ⛔ **No opportunistic autonomy here.** If the need is compelling, ask the user.

{% elif phase._opp and phase._opp.note_kind == 'full' %}
> 🧭 **Opportunistic autonomy — permitted on this action.** You may author and launch an ad-hoc sub-agent (`Task` tool, `general-purpose`/`Explore`, with a prompt you write from context) when you spot a signal the planned agents don't cover — usually in the background. A sub-agent cannot itself spawn sub-agents, so after reading the planned sub-agent's report it is up to you to launch the follow-up.{{ (' ' ~ phase._opp.when|trim) if phase._opp.when else '' }}
{% if phase._opp.examples %}
>
> Examples: {{ phase._opp.examples|join('; ') }}
{% endif %}

{% elif phase._opp and phase._opp.note_kind == 'short' %}
> 🧭 **Opportunistic lead here.** {{ phase._opp.when|trim }}{{ (' — e.g. ' ~ phase._opp.examples|join('; ')) if phase._opp.examples else '' }}

{% endif %}
{% if phase.invocations and phase.invocations|length >= 2 %}
> ⚡ **Parallel — {{ phase.invocations|length }} independent agents** (no order between them). Launch all {{ phase.invocations|length }} in a single message ({{ phase.invocations|length }} `Task` blocks), not one at a time; wait for all to return before moving on.

{% endif %}
{% if phase.type == 'script' %}
```bash
{{ phase.cmd }}
```

{% elif phase.type == 'external' %}
_External tool outside the pipeline. Expected outputs:_{% for out in phase.outputs|default([]) %} `{{ out.path }}` ({{ out.kind }}){% if not loop.last %},{% endif %}{% endfor %}.

{% elif phase.type == 'main_agent' %}
{% if phase.invocations %}
{% for inv_block in phase.rendered_invocations %}
{{ inv_block }}
{% endfor %}
{% endif %}

{% elif phase.type == 'workflow_call' %}
> 🔗 **This phase dispatches another workflow**: `/{{ phase.workflow }}`.
>
> Launch it via the **Skill tool** (`skill: {{ phase.workflow }}`). The target
> workflow runs in the same target/cwd; read its outputs from its usual tree.
> Return to `/{{ skill.name }}` once `/{{ phase.workflow }}` is complete.

{% else %}
{% for inv_block in phase.rendered_invocations %}
{{ inv_block }}
{% endfor %}
{% endif %}

{% if phase.id in manual_blocks_after_phase %}
{% for block in manual_blocks_after_phase[phase.id] %}
{{ block }}

{% endfor %}
{% endif %}

{% endfor %}

{% if on_demand_agents %}
---

## On-demand agents (outside the pipeline)

These agents are available but are **not** invoked automatically in the pipeline.
{% for od in on_demand_agents %}

### `{{ od.agent }}`{% if od.model %} [{{ od.model }}]{% endif %}{% if od.effort and od.effort != 'inherit' %} ⚙️ `{{ od.effort }}` effort{% endif %}{% if od.tools %} 🔧 `{{ od.tools|join(', ') }}`{% endif %}

> {{ od.description|trim|replace('\n', '\n> ') }}

{% if od.when %}**When to invoke it**: {{ od.when }}

{% endif %}
{% if od.triggered_by %}**Triggered by**: {{ od.triggered_by|join(', ') }}

{% endif %}
{% endfor %}

{% endif %}
{% if brainstormings %}
---

## Pipeline brainstormings

{% for bs in brainstormings %}
- **`{{ bs.id }}`** ({{ bs.protocol }}, {{ bs.timebox_minutes }} min) — {% if bs.after_phase %}after `{{ bs.after_phase }}`{% endif %}{% if bs.before_phase %}, before `{{ bs.before_phase }}`{% endif %}.
{% endfor %}

See the shared protocol above.
{% endif %}

{% for block in manual_blocks_at_end|default([]) %}

---

{{ block }}
{% endfor %}
